# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, build with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Compiled binaries
/kbase_sdk_cli
/dist/

# Test project directory (go run main.go init test_project)
/test_project/
